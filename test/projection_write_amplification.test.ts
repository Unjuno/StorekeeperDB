import { test } from "node:test";
import "../scripts/projection_write_amplification_experiment.js";

test("projection write amplification experiment completes with valid measurements", () => {
  // The imported experiment exits non-zero only when correctness or measurement validity fails.
});
